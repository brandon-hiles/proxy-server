{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Proxy Server to filter requests to avoid CORs errors",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/netlify-lambda serve src",
    "build": "./node_modules/.bin/netlify-lambda build src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "netlify-lambda": "^1.6.3",
    "request": "^2.88.2",
    "serverless-http": "^2.3.2",
    "xml": "^1.0.1"
  }
}
